{
  "name": "vtes-italy-judge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VTES Italy judge tool (static site, vanilla JS).",
  "scripts": {
    "test": "node scripts/run-tests.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "minify": "node scripts/minify.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.14.0",
    "@types/node": "^22.19.17",
    "esbuild": "^0.25.12",
    "eslint": "^9.14.0",
    "globals": "^15.12.0",
    "jsdom": "^26.1.0",
    "prettier": "^3.3.3",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
